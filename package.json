{
  "name": "devproxy",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "bin": {
    "devproxy": "./bin/main.js"
  },
  "scripts": {
    "dev": "ts-node-dev --watch --respawn src/main",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^20.10.7",
    "esbuild": "^0.19.11",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "mockttp": "^3.10.1"
  }
}
